{
  "version": "0.2.8",
  "name": "newpaper-api",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint:": "tsc --noEmit && npm publish"
  },
  "dependencies": {
    "react": "^18.3.1"
  }
}
